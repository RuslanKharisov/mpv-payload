services:
  postgres:
    restart: always
    image: postgres:latest
    env_file: .env
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - '5433:5432'
    networks:
      backend-network:

volumes:
  pgdata:

networks:
  backend-network:
    driver: bridge
